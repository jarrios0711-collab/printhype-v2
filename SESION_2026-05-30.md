# PrintHype v2 — Sesión de Trabajo 2026-05-30/31

## Cambios realizados

### 1. Migración SQL completada
- Tablas faltantes agregadas: `filament_profiles`, `user_ai_config`, `viral_campaigns`
- Columna `stock_deducted` en `order_registry`
- Archivo: `supabase/migrations/20260516190000_create_all_tables.sql`

### 2. RLS Policies (nuevo)
- `supabase/migrations/20260530190000_enable_rls.sql`
- Row Level Security en TODAS las tablas
- `user_ai_config` tiene scope por usuario (`auth.uid() = user_id`)

### 3. Valores hardcodeados eliminados
- `src/app/dashboard/orders/[id]/page.tsx` — progreso, ROI, impresora, fechas ahora son dinámicos

### 4. Dependencias versionadas
- `package.json` — 7 packages cambiados de "latest" a versiones concretas

### 5. Activity SVG duplicado eliminado
- `src/app/dashboard/orders/page.tsx` — se importa de lucide-react en vez de recrear el SVG

### 6. Error boundaries agregados
- inventory, ai-lab, viral, projects, settings — cada uno con su `error.tsx`

### 7. RLS policies
- `supabase/migrations/20260530190000_enable_rls.sql`

### 8. Tooltips en toda la app (~40 tooltips nuevos)
- Dashboard, Orders, Order Detail, Inventory, AI Lab, Viral, Projects, Settings, Login

### 9. PWA instalable
- Iconos regenerados como PNG reales (192, 512, 180, SVG)
- `public/manifest.json` — simplificado y válido
- `public/sw.js` — v4 con offline page
- `src/components/PwaRegister.tsx` — banner de instalación
- `src/components/AppInstall.tsx` — sección en Settings
- `src/app/offline/page.tsx` — página offline
- `next.config.mjs` — headers para SW y manifest

### 10. DeepSeek como proveedor de IA
- `src/app/dashboard/settings/page.tsx` — agregado a AI_PROVIDERS
- `src/app/api/ai/stream/route.ts` — agregado a PROVIDER_META
- `src/app/api/user/ai-config/route.ts` — agregado al enum de Zod

### 11. Fix guardado de AI config
- `src/app/api/user/ai-config/route.ts` — upsert con onConflict no funcionaba, reemplazado por update/insert explícito

### 12. Monitoreo de impresoras 3D via Moonraker
- `supabase/migrations/20260530200000_add_printer_ip.sql` — columnas ip_address y port
- `src/app/api/printers/route.ts` — PATCH handler para actualizar IP
- `src/app/dashboard/settings/page.tsx` — UI para configurar IP y puerto (guardado en localStorage)
- `src/app/dashboard/page.tsx` — PrintersMonitor widget (consulta desde el navegador, con fallback a proxy local)
- `scripts/printer-proxy.js` — proxy local para bypass de seguridad del navegador
- `INICIAR_PROXY_IMPRESORAS.bat` — lanzador del proxy
- `src/app/api/printers/status/route.ts` — endpoint server-side (deprecado, no usado)
- `src/lib/moonraker.ts` — utilidad Moonraker (deprecado, no usado)

### 13. Webhooks
- `src/lib/webhook.ts` — triggerWebhook function
- Webhooks globales: order.created, order.status_changed, inventory.added
- Per-user webhook: cada usuario configura su propio webhook (localStorage)
- Header `x-webhook-url` enviado desde frontend en cada acción

### 14. Instalación de app
- `src/components/PwaRegister.tsx` — rewrite con useEffect
- `src/components/AppInstall.tsx` — rewrite con useEffect

---

## Pendientes / Issues conocidos
- Las migraciones SQL no están aplicadas en Supabase (pendiente)
- `scripts/printer-proxy.js` y moonraker.ts están deprecados
- El monitor de impresoras necesita el proxy local o corre desde el navegador
- Layout de Android/iOS testing pendiente
- Service worker cacheo fino pendiente
